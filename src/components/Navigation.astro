---
import { Icon } from 'astro-iconset/components';

const links = [
	{ label: 'Inicio', href: '#inicio', icon: 'lucide:house' },
	{ label: 'Experiencia', href: '#experiencia', icon: 'lucide:user-round' },
	{ label: 'Proyectos', href: '#proyectos', icon: 'lucide:folder' },
	{ label: 'Contacto', href: '#contacto', icon: 'lucide:smartphone' },
];
---

<div class="nav-canvas">
	<nav class="portfolio-nav" aria-label="Navegación principal">
		<ul>
			{links.map((link, index) => (
				<li>
					<a
						href={link.href}
						data-nav-link={link.href.slice(1)}
						class:list={{ active: index === 0 }}
						aria-current={index === 0 ? 'page' : undefined}
						aria-label={link.label}
					>
						<Icon name={link.icon} width="24" height="24" aria-hidden="true" />
						<span>{link.label}</span>
					</a>
				</li>
			))}
		</ul>
	</nav>
</div>
