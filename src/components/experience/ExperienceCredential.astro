---
import { credentials } from '../../data/credentials';
import CredentialCarousel from './CredentialCarousel';
---

<div class="credential-carousel" data-reveal="experience-credentials" data-reveal-order="2">
	<CredentialCarousel credentials={credentials} client:visible />
</div>
